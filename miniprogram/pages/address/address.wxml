<van-cell-group>
  <van-field
    value="{{ username }}"
    required
    clearable
    label="用户名"
    placeholder="请输入用户名"
  />

  <van-field
    value="{{ phone }}"
    type="number"
    label="手机号"
    placeholder="请输入手机号"
    required
    border="{{ false }}"
  />
  <van-field
    value="{{ phone }}"
    type="text"
    label="地址"
    placeholder="请输入地址"
    required
    border="{{ false }}"
  />
   <van-field
    value="{{ phone }}"
    type="text"
    label="详细地址"
    placeholder="请输入详细地址"
    required
    border="{{ false }}"
  >
  <!-- <van-area area-list="{{ areaList }}" /> -->
  </van-field>
</van-cell-group>
<view class="btnBox">
  <van-button type="info" custom-class="sunccessBtn" round>保存</van-button>
</view>